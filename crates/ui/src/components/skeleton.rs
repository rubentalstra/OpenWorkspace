use crate::slot;
use leptos::prelude::*;

slot! {
    /// Skeleton — shadcn Base UI `skeleton`. A pulsing placeholder; set sizing
    /// utilities via `class` at the call site.
    Skeleton, div, "skeleton", "cn-skeleton animate-pulse"
}
